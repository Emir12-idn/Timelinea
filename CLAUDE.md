# Project Instructions

## Current State

This repo has no application code yet — just a README ("Timelinea / Ms schedule") and the
ECC (everything-claude-code) toolkit installed under `.claude/` (skills, agents, commands,
rules). There is no package manifest, framework, build tool, or test suite to detect.

## Tech Stack

Not yet chosen. When the actual app is started, update this section with language,
framework, database, and test runner.

## Project Structure

- `README.md` — one-line project description
- `.claude/` — ECC toolkit (skills, agents, commands, rules); see `.claude/README.md`
  and `.claude/rules/ecc/README.md` for what's installed and how the rule layers work

## Conventions

- Branch naming seen so far: `claude/<slug>` (Claude Code session branches)
- No commit message convention enforced yet beyond ECC's own `git-workflow.md` rule
  (`<type>: <description>`, seen in this repo's own commits)
